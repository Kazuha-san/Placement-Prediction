# Testing dependencies - install with: pip install -r testing/requirements.txt
# (in addition to backend/requirements.txt, which must also be installed)
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.24
httpx>=0.27
asyncpg>=0.29        # Postgres async driver, needed for integration tests
